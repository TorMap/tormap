# dev config overrides production config (docker-compose.yml)
version: '3.8'
services:
  backend:
    image: eclipse-temurin:17-jdk
    working_dir: /usr/src/app
    command: ./gradlew bootRun
    volumes:
      - ./backend/:/usr/src/app
    ports:
      - "8080:8080"

  frontend:
    image: node:16
    working_dir: /usr/src/app
    command: bash -c "yarn && yarn start"
    volumes:
      - ./frontend/:/usr/src/app
    ports:
      - "3000:3000"
