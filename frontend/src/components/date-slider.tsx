import {FunctionComponent} from "react";

interface Props {
    sliderValue: number
    setSliderValue: (n: number) => void
}
export const DateSlider: FunctionComponent<Props> = ({}) => {

    return (
        <p>debug</p>
    )

}